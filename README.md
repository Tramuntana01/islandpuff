Mallorca Cannabis Social Clubs — Landing Page

Files
- `index.html` — The landing page. Replace the placeholder content with your real info and legal text.
- `styles.css` — Simple responsive CSS for layout and form styling.
- `script.js` — Lightweight client-side validation and a placeholder submission handler.

Quick preview
- Open `index.html` directly in your browser (double-click) for a local preview.
- Or run a tiny local server (recommended) from PowerShell:

```powershell
cd "d:\1. Diseño Web\socialclub"
python -m http.server 8000; Start-Process "http://localhost:8000"
```

What to replace
- Swap the placeholder text in `index.html` (the About section and header) with your content.
- Add legal notices & membership rules where appropriate.
- Hook `script.js` form submission to your backend API (replace the console.log with a `fetch` POST).

Next steps I can help with
- Add a simple Node/PHP backend endpoint to receive form submissions.
- Integrate a search/filter UI that reads a JSON list of clubs.
- Add map integration (Leaflet or Google Maps) to show club locations.

If you want any of the next steps, tell me which one and I'll implement it.
